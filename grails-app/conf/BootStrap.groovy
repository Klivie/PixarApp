import uk.ac.shu.webarch.PixarApp.*

class BootStrap {

/* Basic bootstrapping instructions used to add test/reference data to the database when app is run. Is used to test functionality of the app so I've had to create a studio, a couple directors, numerous films, numerous users. */

    def init = { servletContext ->

	//def pixar_studio = Studio.findByStudioName('Pixar Animation Studios') ?: new Studio(studioName:'Pixar Animation Studios', studioYearFounded:'1986', studioHistory:'').save();
	
	//def _director = Director.findByDirectorName('') ?: new Director(directorName:'', directorAge:'', notes:'').save();	

	//def _director = Director.findByDirectorName('') ?: new Director(directorName:'', directorAge:'', notes:'').save();	

	//def _film = Film.findByFilmName('') ?: new Film (filmName:'', filmDescription:'', filmGenre:'', filmLength:'', releaseDate:'').save();
	
	//def _film = Film.findByFilmName('') ?: new Film (filmName:'', filmDescription:'', filmGenre:'', filmLength:'', releaseDate:'').save();

	//def _film = Film.findByFilmName('') ?: new Film (filmName:'', filmDescription:'', filmGenre:'', filmLength:'', releaseDate:'').save();

	//def _film = Film.findByFilmName('') ?: new Film (filmName:'', filmDescription:'', filmGenre:'', filmLength:'', releaseDate:'').save();
	
	//???def _filmActor = Film.findByFilmCharacterPlayed('') ?: new FilmActor (filmCharacterPlayed:'').save();

	//def _actor = Actor.findByActorName('') ?: new Actor (actorName:'', actorAge:'', actorBio:'').save();

	//def _actor = Actor.findByActorName('') ?: new Actor (actorName:'', actorAge:'', actorBio:'').save();

	//def _user = User.findByUserName('') ?: new User (userName:'', userPassword:'', userEmail:'', userAge:'', userBio:'', favouriteFilm:'').save();

	//def _user = User.findByUserName('') ?: new User (userName:'', userPassword:'', userEmail:'', userAge:'', userBio:'', favouriteFilm:'').save();

	//???def _review = Review.findBy('') ?: new Review (rating:'', notes :'').save();
	

    }
    def destroy = {
    }
}
